--- #KATASCONFIG
id: "arrays-hashing"
title: "Arrays & Hashing"
author: "SYSTEM"
tags:
  - arrays
  - hashing
--- #END KATASCONFIG

lessontitle: "Contains Duplicate"
metadata:
  difficulty: 1

content:
  - type: markdown
    text: |
      # Contains Duplicate

      Given an integer array `nums`, return `true` if any value appears **at least twice** in the array, and return `false` if every element is distinct.

      ### Examples
      - **Input:** `nums = [1, 2, 3, 1]`
        **Output:** `true`
      - **Input:** `nums = [1, 2, 3, 4]`
        **Output:** `false`

  - type: codeblock
    language: python
    boilerplate:
      - function:
          name: contains_duplicate
          expects: bool
          inputs: |
            nums = list[int]

    python: |
      def contains_duplicate(nums: list[int]) -> bool:
          # Write your solution here
          pass

  - type: tests
    test_cases:
      - input: [[1, 2, 3, 1]]
        expected: true
      - input: [[1, 2, 3, 4]]
        expected: false
      - input: [[1, 1, 1, 3, 3, 4, 3, 2, 4, 2]]
        expected: true
      - input: [[]]
        expected: false

  - type: hint
    text: |
      You can use a Set data structure to keep track of numbers you have already visited. If you see a number that is already in the Set, it's a duplicate!

---

lessontitle: "Valid Anagram"
metadata:
  difficulty: 1

content:
  - type: markdown
    text: |
      # Valid Anagram

      Given two strings `s` and `t`, return `true` if `t` is an anagram of `s`, and `false` otherwise.

      An **Anagram** is a word or phrase formed by rearranging the letters of a different word or phrase, typically using all the original letters exactly once.

      ### Examples
      - **Input:** `s = "anagram"`, `t = "nagaram"`
        **Output:** `true`
      - **Input:** `s = "rat"`, `t = "car"`
        **Output:** `false`

  - type: codeblock
    language: python
    boilerplate:
      - function:
          name: is_anagram
          expects: bool
          inputs: |
            s = str
            t = str

    python: |
      def is_anagram(s: str, t: str) -> bool:
          # Write your solution here
          pass

  - type: tests
    test_cases:
      - input: ["anagram", "nagaram"]
        expected: true
      - input: ["rat", "car"]
        expected: false
      - input: ["a", "ab"]
        expected: false
      - input: ["", ""]
        expected: true

  - type: hint
    text: |
      An anagram means both strings have the exact same count of each character. You can count occurrences of each character using a dictionary/hash map, or sort the strings and see if they match.

---

lessontitle: "Two Sum"
metadata:
  difficulty: 1

content:
  - type: markdown
    text: |
      # Two Sum

      Given an array of integers `nums` and an integer `target`, return indices of the two numbers such that they add up to `target`.

      You may assume that each input would have **exactly one solution**, and you may not use the same element twice.

      You can return the answer in any order.

      ### Examples
      - **Input:** `nums = [2, 7, 11, 15]`, `target = 9`
        **Output:** `[0, 1]` (since `nums[0] + nums[1] == 9`)
      - **Input:** `nums = [3, 2, 4]`, `target = 6`
        **Output:** `[1, 2]`

  - type: codeblock
    language: python
    boilerplate:
      - function:
          name: two_sum
          expects: list[int]
          inputs: |
            nums = list[int]
            target = int

    python: |
      def two_sum(nums: list[int], target: int) -> list[int]:
          # Write your solution here
          pass

  - type: tests
    test_cases:
      - input: [[2, 7, 11, 15], 9]
        expected: [0, 1]
      - input: [[3, 2, 4], 6]
        expected: [1, 2]
      - input: [[3, 3], 6]
        expected: [0, 1]

  - type: hint
    text: |
      Try using a Hash Map (dictionary) to store each number and its index as you iterate through the list. For each number, calculate its difference from the target (`complement = target - num`). If `complement` is already in the map, you've found your pair!
